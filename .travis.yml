sudo: false
matrix:
  include:
    - os: linux
      compiler: gcc
    - os: linux
      compiler: clang
    - os: osx
      compiler: clang
script:
  - make -f Makefile.unix clean
  - make -f Makefile.unix
