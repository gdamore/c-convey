language: c
sudo: false
matrix:
  include:
    - os: linux
      compiler: gcc
    - os: linux
      compiler: clang
    - os: osx
      compiler: clang
script:
  - ./build.sh clean
  - ./build.sh build
  - ./build.sh test
